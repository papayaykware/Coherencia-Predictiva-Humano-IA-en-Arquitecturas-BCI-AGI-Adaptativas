def generate_dataset(simulator, n_samples=1000):
    X, y = [], []

    for _ in range(n_samples):
        intent = np.random.randint(0, 2)
        eeg = simulator.generate(intent=intent)

        X.append(eeg)
        y.append(intent)

    return np.array(X), np.array(y)
